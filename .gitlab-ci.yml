image: ruby:3.2  # Usa una imagen con Ruby

pages:
  stage: deploy
  script:
    - gem install bundler
    - bundle install
    - bundle exec jekyll build -d public
  artifacts:
    paths:
      - public
  only:
    - main  # Cambia a "master" si es tu rama principal